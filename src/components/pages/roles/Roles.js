import React from 'react'
import { useState, useEffect } from "react";

// Basic = 0, Project Owner = 1, Boss = 2
export function Roles({user}) {
    const [userroll, setUserroll] = useState(false); 

    useEffect(() => { 



    })
    return(
        <main>
            <h1>Hi</h1>
        </main>
    )
}